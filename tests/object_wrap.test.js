const binding = require("../build/Release/object_wrap");

describe("object wrap bindings", () => {
  it(`works fine`, () => {
    /* console.log(new binding.MyObject(9));
    
    const obj = new binding.TestObject(9);
    expect(obj.value).toEquals(9);

    obj.value = 10;
    expect(obj.value).toEquals(10);

    expect(obj.plusOne()).toEquals(11);
    expect(obj.plusOne()).toEquals(12);
    expect(obj.plusOne()).toEquals(13);

    expect(obj.mulpitly().value).toEquals(13);
    expect(obj.mulpitly(10).value).toEquals(130);

    const newObj = obj.mulpitly(-1);
    expect(newObj.value).toEquals(-13); 
    */
  });
});
